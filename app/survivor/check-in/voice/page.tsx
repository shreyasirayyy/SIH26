"use client";
import Link from "next/link";
import { ArrowLeft, Check, Mic, ShieldCheck, Waves } from "lucide-react";
import { useState } from "react";
export default function VoiceCheckInPage() { const [state, setState] = useState("Ready"); return <div className="px-5 pb-10 md:px-10 xl:px-14"><Link href="/survivor/check-in" className="inline-flex items-center gap-2 text-sm font-semibold text-[#75857f]"><ArrowLeft size={16} /> Check-in</Link><div className="mx-auto mt-8 max-w-3xl text-center"><p className="text-xs font-bold uppercase tracking-[.2em] text-[#7e918b]">Voice check-in</p><h1 className="mt-3 font-display text-5xl text-[#172326]">You can speak in your own time.</h1><p className="mt-4 text-lg text-[#63736e]">No script, no right answer. Just a quiet conversation with TAARA.</p><div className="mx-auto mt-12 flex h-56 w-56 items-center justify-center rounded-full bg-[#dcebdd] shadow-[0_0_0_25px_rgba(220,235,221,.45)]"><button onClick={() => setState(state === "Ready" ? "Listening" : state === "Listening" ? "Processing" : state === "Processing" ? "Complete" : "Ready")} className={`flex h-32 w-32 items-center justify-center rounded-full ${state === "Listening" ? "bg-[#e89a78]" : "bg-[#0f766e]"} text-white shadow-xl`}><Mic size={42} /></button></div><h2 className="mt-10 font-display text-3xl text-[#294138]">{state}</h2><p className="mt-3 text-sm text-[#71817b]">{state === "Ready" ? "Tap the microphone when you’re ready." : state === "Listening" ? "TAARA is listening." : state === "Processing" ? "Preparing a gentle reflection." : "Thank you for sharing."}</p><div className="mx-auto mt-8 max-w-lg rounded-2xl bg-[#f4f6ec] p-4 text-left text-xs leading-relaxed text-[#61716b]"><div className="flex items-center gap-2 font-bold text-[#466459]"><ShieldCheck size={15} /> Privacy note</div><p className="mt-2">Voice analysis is optional. For counsellors, the demo can surface speech pace, pause pattern, and prosodic signal — never a diagnosis.</p></div>{state === "Complete" && <div className="mt-6 flex items-center justify-center gap-2 text-sm font-bold text-[#327d70]"><Check size={16} /> Check-in complete</div>}</div></div>; }
