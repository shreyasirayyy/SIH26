"use client";
import Link from "next/link";
import { ArrowLeft, Eye, Hand, Headphones, Leaf } from "lucide-react";
import { useState } from "react";
const steps = [{ n: 5, label: "things you can see", icon: Eye }, { n: 4, label: "things you can feel", icon: Hand }, { n: 3, label: "things you can hear", icon: Headphones }, { n: 2, label: "things you can smell", icon: Leaf }, { n: 1, label: "thing you can taste", icon: Leaf }];
export default function GroundPage() { const [step, setStep] = useState(0); const current = steps[step]; return <div className="px-5 pb-10 md:px-10 xl:px-14"><Link href="/survivor/feel-better" className="inline-flex items-center gap-2 text-sm font-semibold text-[#75857f]"><ArrowLeft size={16} /> Feel better</Link><div className="mx-auto mt-10 max-w-2xl"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-[#7e918b]">5–4–3–2–1 grounding</p><h1 className="mt-3 font-display text-5xl text-[#172326]">Come back to now.</h1></div><span className="font-display text-5xl text-[#d69e2e]">{current.n}</span></div><div className="mt-8 h-2 rounded-full bg-[#e1e8e1]"><div className="h-2 rounded-full bg-[#7faf86] transition-all" style={{ width: `${(step / 5) * 100}%` }} /></div><div className="surface mt-10 rounded-[28px] p-8 text-center md:p-12"><span className="mx-auto flex h-16 w-16 items-center justify-center rounded-2xl bg-[#f2ecd9] text-[#a47730]"><current.icon size={27} /></span><p className="mt-8 text-xs font-bold uppercase tracking-[.18em] text-[#8a9b94]">Notice</p><h2 className="mt-3 font-display text-3xl text-[#263c35]">{current.n} {current.label}</h2><p className="mt-4 text-sm leading-relaxed text-[#6b7b75]">Take your time. You can name them silently or out loud.</p><button onClick={() => setStep((s) => Math.min(4, s + 1))} className="mt-8 rounded-full bg-[#0f766e] px-7 py-3 text-sm font-bold text-white">{step === 4 ? "You’re here" : "I’m ready"}</button></div></div></div>; }
