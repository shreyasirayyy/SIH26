fastapi==0.141.1
uvicorn==0.52.4
joblib==1.6.0
pandas==3.0.5
scikit-learn==1.8.0
numpy==2.5.2
scipy==1.18.1
pydantic==2.13.5
soundfile==0.14.0
python-multipart==0.0.20

# Optional - enables local Whisper transcription (F16) and better
# sentiment/emotion accuracy (text_signals.py). Everything works without
# these too: the code falls back to lexicon-based scoring and the backend's
# Groq Whisper path for transcription. Uncomment to use the local models:
# transformers==5.16.1
# torch==2.14.0